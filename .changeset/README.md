# Changesets

Run `pnpm changeset` for changes that affect package users. Select the semantic
version bump, write a concise release note, and commit the generated Markdown
file with the change.

Changes that only affect tests, documentation, or project tooling do not need a
release changeset.
