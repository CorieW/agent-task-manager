# .

## Rules

- Treat `.agents/skills/agent-task-manager/SKILL.md` as the canonical Agent Task Manager skill and update it whenever package behavior, CLI commands, configuration, lifecycle contracts, or operational guidance changes in a way that affects how the package is used.
- Keep host-specific discovery adapters and metadata aligned with the canonical skill without duplicating its workflow instructions.
